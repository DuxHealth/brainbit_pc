<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:pages="clr-namespace:NeuroDemo.Pages"
             x:Class="NeuroDemo.DeviceInfoPage"
             Title="BrainBit Demo">
    <ContentPage.Content>
        <Grid RowDefinitions="*,40">
        <ScrollView>
            <Label 
                x:Name="DeviceInfoLabel"
                FontSize="Body" 
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand"
                Padding="5"
                Text="{Binding DeviceInfoText}" />
            </ScrollView>
        <pages:DeviceStateView x:Name="DevStateView" BackgroundColor="AliceBlue" Grid.Column="0" Grid.Row="1"></pages:DeviceStateView>
        </Grid>
    </ContentPage.Content>
</ContentPage>